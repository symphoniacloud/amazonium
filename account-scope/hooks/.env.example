
# Stack name for hooks deployment (default: amazonium-hooks)
# Each developer/test run should use a unique name to avoid conflicts in the same AWS account
STACK_NAME=amazonium-hooks-alice

# Note: When using a custom stack name:
# - Hook alias becomes: Amazonium::{StackNameCamelCase}::S3Resource
# - Stack filters automatically adjust to match the custom name
# E.g. when STACK_NAME=amazonium-hooks-alice:
# - Hook alias = AmazoniumHooksAlice::Guard::S3Resource
# - Stack filters = amazonium-examples-alice*, t-amazonium-hooks-alice*
# The first of these should match stack names you should use for example apps, and the
# second is for this project's own remote test stacks
